[package]
name = "prism-atomic-swap"
version = "0.1.0"
edition = "2021"
description = "Cross-chain atomic swap program using hash time-locked contracts (HTLCs)"
license = "Apache-2.0"

[dependencies]
solana-program = "2.0"
borsh = "0.10"
thiserror = "1.0"

[lib]
crate-type = ["cdylib", "lib"]
