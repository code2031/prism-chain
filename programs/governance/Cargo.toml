[package]
name = "prism-governance"
version = "0.1.0"
edition = "2021"
description = "On-chain governance program for the Prism blockchain"
license = "Apache-2.0"

[dependencies]
solana-program = "2.0"
borsh = "0.10"
thiserror = "1.0"

[lib]
crate-type = ["cdylib", "lib"]
